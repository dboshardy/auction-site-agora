<%= form_for(@bid) do |f| %>
  <% if @bid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bid.errors.count, "error") %> prohibited this bid from being saved:</h2>

      <ul>
      <% @bid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>

  <div class="field">
    <%= f.label :buy_now %>  <br>
    <input type="radio" name=<%= :buy_now %> value="true" checked><strong> True</strong><br>
    <input type="radio" name=<%= :buy_now %> value="false"> <strong>False</strong><br><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
