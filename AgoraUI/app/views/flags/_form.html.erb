<%= form_for(@flag) do |f| %>
  <% if @flag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flag.errors.count, "error") %> prohibited this flag from being saved:</h2>

      <ul>
      <% @flag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field"> 
    <%= f.label :flag_type %>  <br>
    <input type="radio" name=<%= :flag_type %> value="Inappropriate" checked><strong> Inappropriate</strong><br>
    <input type="radio" name=<%= :flag_type %> value="Counterfeit"> <strong>Counterfeit</strong><br><br>
  </div>  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
